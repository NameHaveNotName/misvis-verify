:# Platform Evaluation: 腾讯问卷 vs. MisVis Verify Custom System

**Date:** 2026-08-15

---

## 1. 用户提议

为确保问卷可传播性，考虑使用腾讯问卷（或类似通用问卷平台）收集数据。

---

## 2. 腾讯问卷能做什么

腾讯问卷（以及问卷星、金数据、Qualtrics 等通用平台）适合：

- 快速分发链接（微信、邮件、二维码）
- 基础题目类型：单选、多选、量表、填空
- 简单随机显示题目
- 数据导出为 Excel / CSV
- 收集人口统计学信息
- 作为被试招募或筛选入口

---

## 3. 为什么腾讯问卷无法承载本实验

本实验不是普通问卷，而是一个**受控 HCI 实验系统**。以下核心需求腾讯问卷无法满足：

### 3.1 复杂的被试内/被试间设计

本实验需要：
- 每个被试看 12 个 main trials + 4 baseline + 6 transfer
- Main trials 中 6 个 accurate、6 个 misleading
- Main trials 中 6 个 AI provenance、6 个 no provenance
- 同一被试**绝不能**看到同一 matched pair 的两个版本
- 被试间随机分配到 Control / EGVV

腾讯问卷的随机逻辑通常只能做到：
- 在同一题组内随机题目顺序
- 按选项跳转

无法做到基于被试 ID 的确定性 counterbalancing，也无法保证不重复显示同一 pair 的两个版本。

### 3.2 EGVV 四阶段交互流程

EGVV 需要：
- Locate → Explain → Verify → Compare 的分步推进
- 每步记录停留时间（locate_time_ms, explain_time_ms, verify_time_ms, compare_time_ms）
- Compare 步骤动态替换图表为 faithful alternative
- 步骤之间需要被试主动点击"继续"

腾讯问卷不支持这种多状态、带计时、带动态内容替换的交互流程。

### 3.3 精确的响应时间记录

我们需要记录：
- 图表呈现到初始判断的时间
- EGVV 每个步骤的停留时间
- 总 trial 时间

腾讯问卷可以记录问卷总时长，但无法精确到单张图表或单一步骤的毫秒级 RT。

### 3.4 中性的刺激标识与答案防泄漏

实验要求：
- SVG 文件名使用中性 ID（S001.svg、S002.svg）
- 不在 URL 或页面文本中暴露 groundTruth
- 防止被试通过右键查看图片路径推断答案

腾讯问卷上传的图片会带有原始文件名或平台生成的路径，难以完全控制。

### 3.5 图表显示一致性

实验要求：
- 固定最大宽度，保持比例
- 最低分辨率 1280 × 720
- 禁止手机参与（正式实验）

腾讯问卷对移动端友好，但这正是问题所在：大量手机用户会看到严重缩小的图表，破坏实验控制。

### 3.6 刷新恢复与状态保持

本实验需要：
- 刷新页面后从当前 trial 继续
- trial 顺序不变
- 被试条件不变

腾讯问卷刷新后通常会重新从第一题开始或丢失进度。

### 3.7 自定义数据导出格式

本实验需要导出：

```json
misvis-study-MVXXXXXXXX.json
```

包含 session 和 trials 的嵌套结构。腾讯问卷只能导出扁平的 Excel/CSV，无法保留嵌套结构和精确的 RT 字段。

### 3.8 Pilot 反馈与正式模式隔离

本实验需要：
- `?mode=pilot` 允许填写 bug 反馈
- `?mode=study` 在伦理审批前默认关闭

腾讯问卷无法区分 pilot 与正式模式，也无法在界面上明确标注"当前为内部 Pilot"。

---

## 4. 腾讯问卷可以承担的角色

虽然不适合作为主实验平台，腾讯问卷可用于：

### 4.1 被试招募与筛选

发布招募问卷，收集：
- 是否愿意参与
- 联系方式（由研究者后续发送正式实验链接）
- 人口统计学信息（年龄、性别、教育背景）

### 4.2 实验后的人口统计补充

如果希望把人口统计变量从主实验中分离出来，可以在主实验完成后用腾讯问卷收集：
- 年龄
- 性别
- 教育程度
- 数据可视化经验自评

然后将两个数据源通过 participant_id 匹配。

### 4.3 不记名的外部校验

如果未来要把部分量表（如可视化素养量表）外包，可以用腾讯问卷独立收集，再用 participant_id 关联。

---

## 5. 推荐方案

**主实验必须在 MisVis Verify 自定义系统中完成。** 腾讯问卷仅作为辅助工具：

1. **招募阶段：** 用腾讯问卷发布招募信息，被试报名后获得带唯一邀请码的实验链接。
2. **主实验阶段：** 被试在电脑浏览器打开 `study.html?mode=pilot` 或 `study.html?mode=study`。
3. **数据收集：** 主实验数据以 JSON 文件形式由被试下载后返回给研究者，或通过未来的 RemoteStorageAdapter 上传。
4. **补充问卷（可选）：** 主实验结束后，被试填写腾讯问卷补充人口统计信息，研究者通过 participant_id 关联。

---

## 6. 如果坚持使用腾讯问卷做简化版实验

可以做一个**严重简化版**，但会损失大部分研究价值：

- 只测 Provenance 主效应：被试随机看到"带 AI 标签"或"不带标签"的一组图表。
- 取消 EGVV 干预，或把 EGVV 改成一段静态文字说明。
- 取消 transfer phase。
- 用 5 点或 7 点量表替代 slider。

**结论：** 这种简化版无法回答 RQ2 和 RQ3，只能部分回答 RQ1，且内部效度大幅下降。不建议用于正式论文。

---

## 7. 最终建议

- **不采用腾讯问卷作为主实验平台。**
- **采用 MisVis Verify 自定义系统 + 腾讯问卷辅助招募/补充信息。**
- 在论文方法部分明确说明：实验在定制前端系统中进行，以确保刺激控制、counterbalancing 和精确的 RT 记录。
